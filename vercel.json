{
  "version": 2,
  "buildCommand": "cd client && npm ci && npx vite build",
  "outputDirectory": "client/dist",
  "installCommand": "npm install && cd client && npm ci",
  "rewrites": [
    {
      "source": "/api/:path*",
      "destination": "/api"
    }
  ]
}
